diff --git a/arch/x86/cpu/tangier/tangier.c b/arch/x86/cpu/tangier/tangier.c
index 0fbe401..9274b8f 100644
--- a/arch/x86/cpu/tangier/tangier.c
+++ b/arch/x86/cpu/tangier/tangier.c
@@ -112,10 +112,18 @@ void panic_puts(const char *str)
 
 int board_mmc_init(bd_t * bis)
 {
-	int index = 0;
-	unsigned int base = CONFIG_SYS_EMMC_PORT_BASE + (0x40000 * index);
+	unsigned int base0 = 0xff3fc000;
+	unsigned int base1 = 0xff3fa000;
 
-	return tangier_sdhci_init(base, index, 4);
+	/* init emmc */
+	if (tangier_sdhci_init(base0, 0, 4))
+		printf("Error: unable to initialize emmc\n");
+
+	/* init sd card */
+	if (tangier_sdhci_init(base1, 1, 4))
+		printf("Error: unable to initialize sd\n");
+
+	return 0;
 }
 
 /* ovveride get_tbclk_mhz code see tsc_timer */
diff --git a/drivers/mmc/tangier_sdhci.c b/drivers/mmc/tangier_sdhci.c
index 3337651..6644734 100644
--- a/drivers/mmc/tangier_sdhci.c
+++ b/drivers/mmc/tangier_sdhci.c
@@ -10,6 +10,7 @@
 int tangier_sdhci_init(u32 regbase, int index, int bus_width)
 {
 	struct sdhci_host *host = NULL;
+	u32 maxclk;
 
 	host = (struct sdhci_host *)malloc(sizeof(struct sdhci_host));
 	if (!host) {
@@ -25,14 +26,23 @@ int tangier_sdhci_init(u32 regbase, int index, int bus_width)
 	host->quirks = SDHCI_QUIRK_NO_HISPD_BIT | SDHCI_QUIRK_BROKEN_VOLTAGE |
 	    SDHCI_QUIRK_BROKEN_R1B | SDHCI_QUIRK_32BIT_DMA_ADDR |
 	    SDHCI_QUIRK_WAIT_SEND_CMD;
-	host->voltages = MMC_VDD_165_195;	//MMC_VDD_32_33 | MMC_VDD_33_34 | MMC_VDD_165_195;
+
+	if (index == 0) {
+		host->voltages = MMC_VDD_165_195;
+		maxclk = 200000000;
+	}
+	else {
+		host->voltages = MMC_VDD_32_33 | MMC_VDD_33_34;
+		maxclk = 50000000;
+	}
+
 	host->version = sdhci_readw(host, SDHCI_HOST_VERSION);
 
 	host->index = index;
 
 	host->host_caps = MMC_MODE_HC;
 
-	add_sdhci(host, 200000000, 400000);
+	add_sdhci(host, maxclk, 400000);
 
 	return 0;
 }
